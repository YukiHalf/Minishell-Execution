# minishell
This repo contains one of the larger 42 projects: building a functional minishell in a team of two. The goal is to handle input parsing, built-in commands, redirections, pipes, and environment variables, closely mimicking basic shell behavior.
